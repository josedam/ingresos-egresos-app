// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyAAF4etqkEY-KYWVzToqdcP0bmoUNROhLE',
    authDomain: 'ingreso-egrese-app.firebaseapp.com',
    databaseURL: 'https://ingreso-egrese-app.firebaseio.com',
    projectId: 'ingreso-egrese-app',
    storageBucket: 'ingreso-egrese-app.appspot.com',
    messagingSenderId: '306646198452',
    appId: '1:306646198452:web:3226dc5a485b05a4'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
